{
  "name": "experiments-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix Experiment-4/auth-context-app && npm run build --prefix Experiment-4/dark-mode-context && npm run build --prefix Experiment-5 && npm run build --prefix Experiment-6 && node move-builds.js"
  },
  "dependencies": {
    "fs-extra": "^11.3.3"
  }
}
